<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <title>Board Games</title>
</head>
<body>
    <h1>Board Games Collection</h1>

    <div class="row row-cols-1 row-cols-md-4 g-4 text-center mx-auto">
            <% boardgames.forEach(element => { %>
                <div class="col-4">
                    <div class="card" style="width: 18rem;">
                        <img src="<%= element.cover %>" class="card-img-top" alt="...">
                        <div class="card-body">
                            <h5 class="card-title"><%= element.name %></h5>
                            <p class="card-text"><%= element.owned ? 'owned' : 'not owned' %></p>
                            <a href="/<%= element.id %>" class="btn btn-primary" target="_blank">More Info</a>
                        </div>
                    </div>
                </div>
            <% }); %>
    </div>

</body>
</html>